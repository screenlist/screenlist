import styles from '../styles/HeroCard.module.css'

const SearchWidget = ({url}) => {
	
	return(
		<section>
			<p>All of South African cinema live here.</p>
		</section>
	)
}

export default SearchWidget